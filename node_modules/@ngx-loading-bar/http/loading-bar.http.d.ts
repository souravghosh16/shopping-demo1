import { LoadingBarService } from '@ngx-loading-bar/core';
import { ConnectionBackend, Http, Request, RequestOptions, RequestOptionsArgs, Response } from '@angular/http';
import { Observable } from 'rxjs';
export interface LoadingBarRequestOptionsArgs extends RequestOptionsArgs {
    ignoreLoadingBar?: boolean;
}
export declare class LoadingBarHttp extends Http {
    private loadingBar;
    constructor(_backend: ConnectionBackend, _defaultOptions: RequestOptions, loadingBar: LoadingBarService);
    request(url: string | Request, options?: LoadingBarRequestOptionsArgs): Observable<Response>;
}
